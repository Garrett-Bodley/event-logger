<%= form_for @event do |f| %>
  <fieldset>
    <%= f.label :log, "Paste log text here:", class: "label" %>
    <%= f.text_area :log %>
  </fieldset>
  <fieldset>
    <%= f.submit "Save Log Event" %>
  </fieldset>
<% end %>